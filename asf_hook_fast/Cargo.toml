[package]
name = "asf_hook_fast"
version = "0.1.0"
edition = "2021"
description = "ASF fast-path Stage 1 hook — sub-millisecond regex filter before the Python pipeline"

[dependencies]
serde_json = "1"
regex = "1"

[profile.release]
opt-level = 3
strip = true
lto = true
codegen-units = 1
